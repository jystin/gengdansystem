上线指南（微信小程序）

概览
- 该仓库为演示用的跟单系统（本地 mock 数据）。上线前请确保移除或替换所有演示/调试捷径，并迁移鉴权与数据存储到后端云服务（云函数 + 数据库 + openid）。

一、准备工作（必做）
1. 注册并配置微信小程序账号，获取 AppID。生产环境请使用真实 AppID。
2. 检查 `miniprogram/app.json`、`project.config.json` 中的 appid 与环境配置是否正确。
3. 在代码中搜索并移除/标注演示快捷方式（如 `wx.setStorageSync('sessionUserId','u-root')` 等），以免误发布。
4. 将 mock 数据替换为后端接口（建议使用云函数 + 数据库），并把权限校验下移到后端。

二、在微信开发者工具中本地发布（测试）
1. 打开微信开发者工具，导入项目目录 `跟单系统`，选择对应 AppID（测试号或体验号）。
2. 点击“编译”查看页面展示是否正常。
3. 在“详情”→“上传”中上传代码包（需填写版本号、描述）。
4. 在小程序管理后台提交审核并发布（仅生产环境需要审核，体验号可直接体验）。

三、把手机设置为超级管理员（两种方法）
方法 A（推荐，UI 操作）
- 使用超级管理员账号登录后，进入“个人中心”页面的“切换演示身份”下拉，选择 `我`（或显示为超级管理员用户）。
- 页面会调用 `app.setCurrentUser()` 切换到该用户并将 session 写入 `wx.setStorageSync('sessionUserId', ...)`。

方法 B（开发者快速设置，开发环境专用）
- 在微信开发者工具 → Console 中执行：

```javascript
wx.setStorageSync('sessionUserId', 'u-root')
// 然后重新打开小程序或手动调用 app.syncAccessContext()
```

注意：方法 B 仅用于本地演示；上线前请删除或在发布说明中标注为仅测试用。

四、邀请他人加入（管理员流程）
1. 管理员登录后打开管理中心（`/pages/admin/index`）。
2. 使用“分享入驻”按钮或“复制入驻链接”将包含入驻码的链接发给员工（链接格式示例：`/pages/join/index?invite=JOIN-20260516`）。
3. 员工通过该链接或扫码进入申请页，填写姓名与岗位并提交（状态为 `pending`）。
4. 管理员在管理中心的“待审批员工”列表中审批通过，员工状态变为 `active` 并可使用系统。

五、上线前建议清单
- 移除或保护演示邀请码（`SYSTEM_JOIN_INVITE_CODE`）和本地 mock 数据入口。
- 将权限判断（isAdmin/isSuperAdmin）转移到后端，并使用 openid 或 JWT 做鉴权。
- 审计所有写操作：创建/修改/删除员工、修改库存、撤回工序等，均需后台校验操作者权限并写入审计日志。
- 检查文件上传、文档导出等功能在生产环境下的可用性与权限控制。

六、常见问题与排查
- 登录失败：检查本地 `deviceId` 与 `sessionUserId` 是否被意外清除；可通过开发者工具手动设置。
- 申请后无法进入：请确认管理员已审批该账号，并检查 `state.employees` 中该用户的 `status` 字段是否为 `active`。

---
变更记录（本次审查）
- 修复 `createOrder` 对工序选择字段的兼容（支持 `stepKeys` 与 `selectedStepKeys` 两种命名）。
- 修复 `updateEmployeeRole` 在修改权限时未确保 `employee.stations` 为数组的异常。
- 为若干页面新增左上角“回首页”悬浮按钮（方便演示）。
- 在 `home` 页面保存岗位修改时，改为使用 `app.globalData.currentUser.id` 作为操作者 ID，避免回退到硬编码的 `u-admin`。

如果需要，我可以继续：
- 帮你把演示 mock 层迁移到云函数 + 数据库的示例实现；
- 在发布包中移除开发/演示快捷方式；
- 或者生成逐步的审核提交流程文档（包含截图）。
